export type CarProps = {
  name: string;
  color: string;
};

export type responseProps = {
  name: string;
  color: string;
  id: number;
};

export type winnerResponse = {
  id: number;
  wins: number;
  time: number;
};

export type firstWinner = {
  carName: string;
  time: string;
};
