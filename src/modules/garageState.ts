import { responseProps, winnerResponse, firstWinner } from '../types/types';
export const garageState: responseProps[] = [];
export const winnersState: winnerResponse[] = [];
export const firstCarState: firstWinner[] = [];
